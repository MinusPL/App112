<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:App112"
			 x:Class="App112.RegisterPage"
			 Title="App112">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<ScrollView Grid.Row="1">
			// left, top, right, bottom
			<StackLayout Orientation="Vertical" Padding="16,6,16,16" Spacing="10">
				<Entry Placeholder="Imię"
					   Keyboard="Text"/>
				<Entry Placeholder="Nazwisko"
					   Keyboard="Text"/>
				<Entry Placeholder="Telefon"
					   Keyboard="Telephone" />
				<Button Text="Zarejestruj"
						Clicked="BtnRegisterClick"
						BackgroundColor="{StaticResource Primary}"
						TextColor="White"/>
			</StackLayout>
		</ScrollView>
	</Grid>
</ContentPage>